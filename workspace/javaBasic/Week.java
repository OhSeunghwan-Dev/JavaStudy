package javaBasic;

public enum Week {
	MONDAY,
	TUESDAY,
	WEDNESDAY,
	THURSDAY,
	FRIDAY,
	SATERDAY,
	SUNDAY
}

// Week 라는 이름으로 Enum 객체가 만들어진다.
// 변하지 않는 상수로만 이루어져 있다.
// 한정된 값만 갖는다.
// 메인 메서드가 없다. Enum 객체가 호출 당해야만 객체가 갖고 있는 상수를 사용할 수 있다.